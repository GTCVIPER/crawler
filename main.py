# 这是一个示例 Python 脚本。

# 按 Shift+F10 执行或将其替换为您的代码。
# 按 双击 Shift 在所有地方搜索类、文件、工具窗口、操作和设置。


def print_hi(name):
    # 在下面的代码行中使用断点来调试脚本。
    print(f'Hi, {name}')  # 按 Ctrl+F8 切换断点。


# 按间距中的绿色按钮以运行脚本。
if __name__ == '__main__':
    print_hi('PyCharm')

# 访问 https://www.jetbrains.com/help/pycharm/ 获取 PyCharm 帮助

# # 定位需要处理的页面元素
                # as_ = response.css('a')
                # for ab in as_:
                #     a_url = ab.attrib.get('href')
                #     if a_url:
                #         ab_url = overwriteURL(a_url, url_path_, self.start_domain, sub_domain)
                #         ab.attrib['href'] = ab_url
                #
                # images = response.css('img')
                # for is_ in images:
                #     igs_url = is_.attrib.get('src')
                #     if igs_url:
                #         igs_url_ = overwriteURL(igs_url, url_path_, self.start_domain, sub_domain)
                #         is_.attrib['src'] = igs_url_
                #
                #
                # scripts = response.css('script[src]')
                # for ss in scripts:
                #     ss_url = ss.attrib.get('src')
                #     if ss_url:
                #         ss_url_ = overwriteURL(ss_url, url_path_, self.start_domain, sub_domain)
                #         ss.attrib['src'] = ss_url_
                #
                #
                # links = response.css('link[href]')
                # for ls in links:
                #     ls_url = ls.attrib.get('href')
                #     if ls_url:
                #         ls_url_ = overwriteURL(ls_url, url_path_, self.start_domain, sub_domain)
                #         ls.attrib['href'] = ls_url_